// Флаги разделов. Архивные разделы скрыты из навигации и маршрутов, но их КОД на
// месте (страницы, компоненты, контексты не удалены) — возврат = переключить флаг в true.
// Письма: при false скрыт раздел/страница/навигация, НО способность ИИ писать письма
// (MailModal + send_email) остаётся рабочей — она не завязана на этот флаг.
export const MAIL_ENABLED = false
export const HISTORY_ENABLED = false
